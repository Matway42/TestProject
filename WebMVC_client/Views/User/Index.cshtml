@model IEnumerable<WebMVC_client.Models.User>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@foreach (var m in Model)
{
    <div>@m.Name</div>
    <div style="padding-bottom: 30px">@m.LastName</div>
    
}

